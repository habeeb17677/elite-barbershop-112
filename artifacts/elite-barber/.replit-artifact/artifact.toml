kind = "web"
previewPath = "/"
title = "Elite Barbershop"
version = "1.0.0"
id = "artifacts/elite-barber"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 25836

[services.development]
run = "pnpm --filter @workspace/elite-barber run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/elite-barber", "run", "build" ]
publicDir = "artifacts/elite-barber/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "25836"
BASE_PATH = "/"
